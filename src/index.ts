async function main() {
  console.log("Starting LinkLab CRE + x402 + AI agent...");

  // TODO: load env, connect CRE, init modules, start agent loop
}

main().catch((err) => {
  console.error("Fatal error:", err);
  process.exit(1);
});
